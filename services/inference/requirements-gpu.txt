# GPU extras — install on Ubuntu GPU VM only
# RACE unified venv: use requirements-race-gpu.txt (numpy<2 for Paddle) via scripts/race-setup-venv.sh
-r requirements.txt
torch>=2.5.0
ultralytics>=8.3.0
opencv-python-headless>=4.10.0
segmentation-models-pytorch>=0.5.0
pdf2image>=1.17.0
supabase>=2.11.0
